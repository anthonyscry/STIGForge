<Window x:Class="STIGForge.App.Views.PackComparisonDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Packs to Compare" Height="320" Width="600" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Header -->
    <TextBlock Grid.Row="0" Text="Select two packs to compare:" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,16"
               Foreground="{DynamicResource TextPrimaryBrush}"/>

    <!-- Pack Selection -->
    <Grid Grid.Row="1">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="16"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

       <!-- Baseline Pack -->
       <TextBlock Grid.Row="0" Text="Baseline Pack (older version):" FontWeight="SemiBold" Margin="0,0,0,4"/>
       <ListBox Grid.Row="1" x:Name="BaselineListBox" DisplayMemberPath="Name" SelectionMode="Single"
                ItemsSource="{Binding AvailablePacks}"
                VirtualizingStackPanel.IsVirtualizing="True"
                VirtualizingStackPanel.VirtualizationMode="Recycling"
                ScrollViewer.CanContentScroll="True"/>

       <!-- Target Pack -->
       <TextBlock Grid.Row="3" Text="Target Pack (newer version):" FontWeight="SemiBold" Margin="0,0,0,4"/>
       <ListBox Grid.Row="4" x:Name="TargetListBox" DisplayMemberPath="Name" SelectionMode="Single"
                ItemsSource="{Binding AvailablePacks}"
                VirtualizingStackPanel.IsVirtualizing="True"
                VirtualizingStackPanel.VirtualizationMode="Recycling"
                ScrollViewer.CanContentScroll="True"/>
    </Grid>

    <!-- Buttons -->
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button Content="Compare" Width="100" Height="32" Margin="0,0,8,0" Click="Compare_Click" IsDefault="True"/>
      <Button Content="Cancel" Width="100" Height="32" Click="Cancel_Click" IsCancel="True"/>
    </StackPanel>
  </Grid>
</Window>
